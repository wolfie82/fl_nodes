name: example
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

dependencies:
  flutter:
    sdk: flutter
  fl_nodes:
    path: ../
  gap: ^3.0.1
  provider: ^6.1.2
  uuid: ^4.5.1
  file_picker: ^8.1.7

flutter:
  uses-material-design: true